import { defineDivider, DividerProps, iDivider } from '@lun-web/components';
import createComponent from '../createComponent19';

export const LDivider = createComponent<DividerProps, iDivider>('divider', defineDivider);
if (__DEV__) LDivider.displayName = 'LDivider';
