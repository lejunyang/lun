import { defineSpin, SpinProps, iSpin } from '@lun-web/components';
import createComponent from '../createComponent19';

export const LSpin = createComponent<SpinProps, iSpin>('spin', defineSpin);
if (__DEV__) LSpin.displayName = 'LSpin';
