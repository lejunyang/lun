import { defineSelectOptgroup, SelectOptgroupProps, iSelectOptgroup } from '@lun-web/components';
import createComponent from '../createComponent19';

export const LSelectOptgroup = createComponent<SelectOptgroupProps, iSelectOptgroup>('select-optgroup', defineSelectOptgroup);
if (__DEV__) LSelectOptgroup.displayName = 'LSelectOptgroup';
