import { defineTeleportHolder, TeleportHolderProps, iTeleportHolder } from '@lun-web/components';
import createComponent from '../createComponent19';

export const LTeleportHolder = createComponent<TeleportHolderProps, iTeleportHolder>('teleport-holder', defineTeleportHolder);
if (__DEV__) LTeleportHolder.displayName = 'LTeleportHolder';
